#include <stdio.h>

int main() {
    int a = 4, b = 6, result;
    result = (a < b && printf("Jenny")) || printf("Lectures");
    printf("%d\n", result); // Added newline for better readability
    return 0; // Return statement for standard compliance
}
