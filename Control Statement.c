/* if
if else
    if else- if sudden
    nested if
    switch
    */
#include <stdio.h>

void main() {
    int a, b, sum;
    printf("Enter a & b: ");
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("sum = %d\n", sum);
    printf("Don't want to calculate sum\n");
}

